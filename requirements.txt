flask>=2.0.0
flask-cors>=3.0.10
scikit-learn>=1.0.0
numpy>=1.21.0
gunicorn>=20.1.0

# Optional: Advanced features (comment out if not needed)
dnspython>=2.0.0
python-whois>=0.8.0

# Production dependencies
Werkzeug>=2.0.0
requests>=2.28.0
APScheduler==3.10.4
psutil>=5.9.0
python-dotenv>=1.0.0

# AI Chatbot (supports both FREE Hugging Face and PAID OpenAI)
# requests is already included above for Hugging Face
# openai is optional - only needed if using OpenAI provider
# openai>=1.0.0
